<!doctype html>
<html>
<head><meta charset="utf-8" /><title><%= client.name %> — Lead Form</title></head>
<body>
  <h2><%= client.name %> — Lead Form</h2>

  <% if (paused) { %>
    <p style="color:red;"><b>This client is currently paused.</b></p>
    <p>Please try again later.</p>
  <% } else { %>
    <form method="post" action="/c/<%= client.code %>/lead">
      <input name="name" placeholder="Your Name" required /><br/><br/>
      <input name="email" placeholder="Your Email" required /><br/><br/>
      <input name="phone" placeholder="Phone (optional)" /><br/><br/>
      <textarea name="message" placeholder="Message (optional)"></textarea><br/><br/>
      <button type="submit">Send</button>
    </form>
  <% } %>
</body>
</html>
